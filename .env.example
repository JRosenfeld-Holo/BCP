# Copy to .env.local for local dev; set the real values in the Vercel dashboard.
# These are server-side only — never prefix them with VITE_, or Vite will inline
# them into the public client bundle.
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=
